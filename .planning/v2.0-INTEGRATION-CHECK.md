---
milestone: v2.0
checked: 2026-04-30
phases: [04-message-capture-persistence, 06-thread-summary-pipeline]
wired: 14
orphaned: 2
missing: 0
e2e_flows: { complete: 5, broken: 0, blocked_on_phase_0_ops: 1 }
---

# v2.0 Cross-Phase Integration Check

## Wiring Summary

- **Connected:** 14 exports properly used across phases
- **Orphaned:** 2 exports (`upsertThreadTitle`, `isThreadSummaryPublishedToday`) — see below
- **Missing:** 0 expected connections not found

## Cross-Phase Wiring

| ID | Check | Status | Evidence |
|----|-------|--------|----------|
| W-1 | Migration v2 appended to MIGRATIONS array, idempotent | PASS | `src/services/db.service.ts:67-73`; loop at line 142 skips already-applied |
| W-2 | `tracked_threads.title` UPDATE compatible with v1 schema | PASS | Migration v2 adds nullable `title TEXT`; `upsertThreadTitle` at `tracked-threads-store.ts:61` runs UPDATE only |
| W-3 | `listTracked()` returns title to Phase 6 orchestrator | PASS | `tracked-threads-store.ts:24` SELECT includes title; mapping at line 52; `TrackedThread.title: string \| null` in `types/index.ts:78` |
| W-4 | Lazy `??=` prepared-statement pattern preserved | PASS | 4 statement slots in `message-store.ts:9-17`; all use `??=` (lines 33, 52, 59, 83); `_resetMessageStoreForTests()` line 187 nullifies all four |
| W-5 | `startScheduler/stopScheduler` public API unchanged | PASS | Exports at `cron.ts:114, 121`; consumers `index.ts:22, 42` unchanged; Map refactor internal |
| W-6 | `listTrackedThreadIds()` is orchestrator's source | PASS | `thread-summary.service.ts:8, 98` |
| W-7 | `ai.service.ts` (filterArticles) byte-identical | PASS | Last commit `b35a222` (v1.0 quick-fix); no Phase 6 commits; `summarizer.service.ts` does not import `ai.service` |
| W-8 | `telegram.ts sendMessageWithRetry` unchanged | PASS | Last commits `981d52f` + `6b52bcb` (v1.0); `thread-summary.sender.ts:4, 20` reuses |
| W-9 | digest.service.ts re-exports for back-compat | PASS | `digest.service.ts:28` re-exports readState/writeState/isDigestPublishedToday; `bot.ts:5-8` consumes |

## E2E Flows

| Flow | Status | Notes |
|------|--------|-------|
| A. Boot sequence (initDb → loadTrackingWhitelist → startScheduler → bot.start + onStart preflight) | PASS (static) | `index.ts:15-32`; correct order |
| B. Capture flow (Grammy filter → 5-step guard chain → upsertMessage) | PASS (static) | `capture.handler.ts` full chain present; try/catch wraps body (REL-04) |
| C. Thread-summary pipeline (cron 06:30 MSK → runThreadSummaryPipeline → per-thread loop → format → split → send → state merge) | PASS (static) | `cron.ts:76-102` + `thread-summary.service.ts` per-thread try/catch |
| D. Idempotency double-fire (WR-03 fix) | PASS (static) | Single `readState()` at orchestrator line 76; passed to `isThreadSummaryPublishedTodayWithState(prevState)` at line 89 |
| E. Coexistence digest 06:00 + thread-summary 06:30 (state merge-write) | PASS (static) | Both pipelines use `{...prevState, ...}` pattern; atomic `renameSync` |
| F. Graceful shutdown (stopScheduler 3 logs → bot.stop → closeDb WAL checkpoint) | PASS (static) / BLOCKED-Phase-0-Ops | Live WAL clearing requires running bot |

## Integration Gaps

None found. All expected cross-phase connections wired and confirmed.

## Orphaned Exports

| Export | Location | Status | Notes |
|--------|----------|--------|-------|
| `upsertThreadTitle` | `tracked-threads-store.ts:60` | Production-orphaned | Was Phase 5 callsite; Phase 5 cancelled. Tested and inert. |
| `isThreadSummaryPublishedToday` | `state.service.ts:102` | Superseded | WR-03 fix moved orchestrator to `isThreadSummaryPublishedTodayWithState`. Dead export. |

## Requirements Integration Map

| REQ-ID | Integration Path | Status |
|--------|------------------|--------|
| SETUP-05/06/07/08 | Docker/ENV → config.ts → throws on missing | WIRED |
| SETUP-09 | Manual gate (BotFather, admin, GDPR) | BLOCKED — Phase-0-Ops pending |
| PRIV-04 | Manual gate (GDPR announcement screenshot/URL) | BLOCKED — Phase-0-Ops pending |
| MSG-01..08 | Grammy filter → capture.handler → mapper → upsertMessage | WIRED |
| STORE-01..04 | initDb → WAL → migrations → schema_migrations → lazy ??= prepared | WIRED |
| REL-04 | capture.handler try/catch wraps full body | WIRED |
| SUM-01..07 | summarizer.service pure function | WIRED |
| AI-07 | filterArticles signature unchanged | WIRED |
| DLV-06..10 | cron 06:30 MSK → orchestrator → formatter → splitter → sender → state merge | WIRED |
| STATE-01..02 | writeState atomic renameSync; readState throws on corrupt | WIRED |
| SCHED-01..04 | Map registry, 3 named jobs, named stop-log, per-job try/catch | WIRED |

## Tech Debt (NOT gaps)

1. **retention-sweep stub** — `cron.ts:108-110` logs INFO and returns. Daily 04:00 MSK cron fires but does nothing. Owner phase (Phase 7) removed.
2. **`/summary`, `/dev-summary` commands absent** — Phase 7 removed; no operator-triggered manual summary path. Only 06:30 MSK cron.
3. **`/storage`, `/forget-me` commands absent** — Phase 7 removed; PRIV-01 forget-me write path does not exist (forgotten_users guard at capture is active, but nothing writes to that table).
4. **`upsertThreadTitle` never called in production** — Phase 5 cancelled; thread titles remain NULL; `refreshThreadTitle` fallback `Тред #N` is the de-facto display label.
5. **`isThreadSummaryPublishedToday` dead export** — Superseded by WithState variant; harmless but unused.
6. **`.env.example` `MESSAGE_RETENTION_DAYS=2`** — Dev override committed; below `min=7` config guard would throw at startup. Misleading as example.
7. **`tracking.service.ts` stale comments** — Lines 37-38, 44-46 reference cancelled Phase 5 and removed Phase 7.

---

_Generated by gsd-integration-checker, 2026-04-30_

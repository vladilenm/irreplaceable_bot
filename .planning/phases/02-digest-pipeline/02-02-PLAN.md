---
phase: 02-digest-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prompts/curator.md
  - src/services/ai.service.ts
autonomous: true
requirements: [AI-01, AI-02, AI-05]
user_setup:
  - service: anthropic
    why: "AI filtering for digest curation"
    env_vars:
      - name: AI_API_KEY
        source: "Anthropic Console -> API Keys (or OpenAI Dashboard -> API Keys)"
    dashboard_config: []

must_haves:
  truths:
    - "AI curator prompt is stored in prompts/curator.md, editable without code changes"
    - "ai.service.ts supports both Claude and OpenAI, switching via AI_MODEL env var"
    - "filterArticles() accepts articles array and system prompt, returns LLM-generated text"
    - "LLM output is ready-made Telegram post text, not structured JSON"
  artifacts:
    - path: "prompts/curator.md"
      provides: "System prompt for AI curator"
      contains: "AI-куратор"
    - path: "src/services/ai.service.ts"
      provides: "filterArticles function with dual-provider support"
      exports: ["filterArticles"]
  key_links:
    - from: "src/services/ai.service.ts"
      to: "@anthropic-ai/sdk"
      via: "npm dependency"
      pattern: "Anthropic"
    - from: "src/services/ai.service.ts"
      to: "openai"
      via: "npm dependency"
      pattern: "OpenAI"
    - from: "src/services/ai.service.ts"
      to: "src/config.ts"
      via: "config.aiModel, config.aiApiKey"
      pattern: "config\\.aiModel"
---

<objective>
Create the AI filtering layer: curator prompt file and ai.service.ts with Claude/OpenAI dual-provider support.

Purpose: The AI service is the intelligence layer that transforms 20-40 raw articles into a curated 3-5 item digest post. It must support provider switching (per D-08, D-09) and return ready-made post text (per D-07, D-10).
Output: prompts/curator.md with the full system prompt, ai.service.ts replacing the empty stub with real Claude + OpenAI implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/rss.md

<interfaces>
From src/config.ts:
```typescript
export const config: BotConfig = {
  botToken: requireEnv('BOT_TOKEN'),
  targetChatId: requireEnv('TARGET_CHAT_ID'),
  aiRadarThreadId: requireEnv('AI_RADAR_THREAD_ID'),
  digestCron: process.env['DIGEST_CRON'] ?? '0 6 * * *',
  aiApiKey: requireEnv('AI_API_KEY'),
  aiModel: process.env['AI_MODEL'] ?? 'claude-sonnet-4-20250514',
  logLevel: process.env['LOG_LEVEL'] ?? 'info',
  nodeEnv: process.env['NODE_ENV'] ?? 'production',
};
```

From src/types/index.ts (will be extended by Plan 01 with RawArticle):
```typescript
export interface RawArticle {
  title: string;
  description: string;
  link: string;
  source: string;
  sourceKey: string;
  pubDate: Date;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create curator prompt file</name>
  <files>prompts/curator.md</files>
  <read_first>
    - docs/rss.md (exact system prompt text in "Системный промпт: AI-куратор" section — use as-is per D-05)
  </read_first>
  <action>
Create `prompts/curator.md` per D-06, containing the EXACT system prompt from rss.md's "Системный промпт: AI-куратор" section (per D-05 — use as-is).

The file should contain the full prompt text starting from "# РОЛЬ" through "# ТОНАЛЬНОСТЬ" and its content. Copy verbatim — do not modify the prompt. The file is plain text (the .md extension is for editability, not for Telegram rendering).

The complete prompt from rss.md:
```
# РОЛЬ
Ты — AI-куратор новостного дайджеста для "Клуба Незаменимых".
Клуб — закрытое сообщество, где профессионалы строят персональные
AI-системы из агентов. Философия: "Система > Навык".

# АУДИТОРИЯ
Разработчики, маркетологи, предприниматели — от промптов к агентам
и оркестрации. Им важна практическая ценность, а не хайп.

# ЗАДАЧА
Из предоставленного списка статей (заголовок + описание + ссылка)
выбери 3-5 самых значимых за последние сутки.

# КАТЕГОРИИ (обязательно пометь каждую новость)
🤖 Агенты — фреймворки, инструменты, кейсы использования AI-агентов
🔗 Оркестрация — мультиагентные системы, пайплайны, MCP, A2A
🧠 Модели — новые релизы LLM, бенчмарки, обновления
🛠 Инструменты — Cursor, Claude Code, IDE, dev tools, обновления продуктов
⚡ Технологии — продукты, инструменты, индустрия AI
💰 Бизнес — инвестиции, экономика, деньги, стартапы, рынок

# КВОТА ПО ИСТОЧНИКАМ
- Из vc.ru — ровно 2 новости в каждом дайджесте (бизнес, деньги,
  экономика, инвестиции в AI/tech). Если за сутки на vc.ru нет
  достойных бизнес-новостей — бери 1, но не 0.
- Остальные 1-3 новости — из технических источников (Habr, OpenAI,
  Anthropic, Hugging Face, LangChain, Cursor, Tproger, VentureBeat).

# КРИТЕРИИ ОТБОРА (приоритет)
1. Можно применить СЕГОДНЯ (практичность > теория)
2. Меняет правила игры (новая модель, новый фреймворк, прорыв)
3. Влияет на "незаменимых" (автоматизация, рычаги, скорость)
4. Контринтуитивность (не очевидно, удивляет)

# АНТИКРИТЕРИИ (отсеивать)
- Маркетинговые пресс-релизы без содержания
- "ИИ заменит всех" без конкретики
- Статьи-обзоры "Топ-10 нейросетей" без глубины
- Дублирующие новости (оставь первоисточник)

# ФОРМАТ ВЫХОДА
Telegram-пост. Русский язык. Без markdown-заголовков (Telegram
не рендерит). Emoji для категорий. Краткость > развёрнутость.

Структура:

📡 AI-радар | [дата]

[emoji категории] [Заголовок на русском — ёмко, 5-8 слов]
[Суть в 1-2 предложениях: что случилось + почему это важно
для человека, который строит AI-систему]
→ [ссылка]

[повторить для 3-5 новостей]

———
Дайджест Клуба Незаменимых
Система > Навык

# ТОНАЛЬНОСТЬ
Прямая, без восторгов. Как разведка докладывает штабу.
Не "невероятный прорыв!", а "вышло — вот что меняет".
```
  </action>
  <verify>
    <automated>test -f prompts/curator.md && grep -c "AI-куратор" prompts/curator.md</automated>
  </verify>
  <acceptance_criteria>
    - prompts/curator.md exists
    - prompts/curator.md contains "# РОЛЬ"
    - prompts/curator.md contains "AI-куратор"
    - prompts/curator.md contains "# КВОТА ПО ИСТОЧНИКАМ"
    - prompts/curator.md contains "vc.ru"
    - prompts/curator.md contains "# ТОНАЛЬНОСТЬ"
    - prompts/curator.md contains "Система > Навык"
    - prompts/curator.md contains "3-5"
  </acceptance_criteria>
  <done>prompts/curator.md contains the exact system prompt from rss.md, editable without code changes</done>
</task>

<task type="auto">
  <name>Task 2: Implement AI service with Claude and OpenAI support</name>
  <files>src/services/ai.service.ts</files>
  <read_first>
    - src/services/ai.service.ts (current empty stub to replace)
    - src/config.ts (config.aiApiKey, config.aiModel usage)
    - src/types/index.ts (RawArticle type for input formatting)
    - src/utils/logger.ts (logging pattern)
    - docs/rss.md (understand that LLM returns ready-made post text per D-07/D-10)
  </read_first>
  <action>
Install official SDKs per D-08:
```bash
npm install @anthropic-ai/sdk openai
```

Replace the empty stub `src/services/ai.service.ts` with a real implementation:

1. Imports:
```typescript
import Anthropic from '@anthropic-ai/sdk';
import OpenAI from 'openai';
import { readFileSync } from 'node:fs';
import { config } from '../config.js';
import { logger } from '../utils/logger.js';
import type { RawArticle } from '../types/index.js';
```

2. Load curator prompt at module level:
```typescript
const curatorPrompt = readFileSync(
  new URL('../../prompts/curator.md', import.meta.url),
  'utf-8',
);
```

3. Helper to format articles for LLM input. Create a function `formatArticlesForLLM(articles: RawArticle[]): string` that formats each article as:
```
---
Источник: {article.source}
Заголовок: {article.title}
Описание: {article.description}
Ссылка: {article.link}
Дата: {article.pubDate.toISOString()}
```
Articles separated by blank lines.

4. Provider detection: check if `config.aiModel` starts with "claude" or "gpt"/"o" to determine provider.

```typescript
function isClaude(model: string): boolean {
  return model.startsWith('claude');
}
```

5. Export async function `filterArticles(articles: RawArticle[]): Promise<string>` per D-09:
   - Format articles using the helper
   - Build user message: `"Сегодняшняя дата: ${new Date().toLocaleDateString('ru-RU')}.\n\nСтатьи для анализа:\n\n${formattedArticles}"`
   - If `isClaude(config.aiModel)`:
     - Create Anthropic client: `new Anthropic({ apiKey: config.aiApiKey })`
     - Call `client.messages.create({ model: config.aiModel, max_tokens: 2000, system: curatorPrompt, messages: [{ role: 'user', content: userMessage }] })`
     - Extract text from response: `response.content[0]` — check type is 'text', return `.text`
   - Else (OpenAI):
     - Create OpenAI client: `new OpenAI({ apiKey: config.aiApiKey })`
     - Call `client.chat.completions.create({ model: config.aiModel, max_tokens: 2000, messages: [{ role: 'system', content: curatorPrompt }, { role: 'user', content: userMessage }] })`
     - Extract text: `response.choices[0]?.message?.content ?? ''`
   - Log: `logger.info({ model: config.aiModel, inputArticles: articles.length }, 'AI filtering complete')`
   - Return the text string (ready-made Telegram post per D-07/D-10)

Note: The function signature is `filterArticles(articles: RawArticle[])` — it reads the prompt from the file internally, not as a parameter. This simplifies the API per D-09 intent while keeping the prompt external per D-06.

Ensure no `any` types. Use proper type narrowing for Anthropic response content blocks.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - @anthropic-ai/sdk appears in package.json dependencies
    - openai appears in package.json dependencies
    - src/services/ai.service.ts contains "export async function filterArticles"
    - src/services/ai.service.ts contains "Anthropic" (Claude SDK usage)
    - src/services/ai.service.ts contains "OpenAI" (OpenAI SDK usage)
    - src/services/ai.service.ts contains "config.aiModel" (provider switching)
    - src/services/ai.service.ts contains "config.aiApiKey" (key from config)
    - src/services/ai.service.ts contains "curator" (prompt file reference)
    - src/services/ai.service.ts contains "curatorPrompt" or "curator.md"
    - src/services/ai.service.ts does NOT contain ": any" (strict TypeScript)
    - src/services/ai.service.ts contains "RawArticle" (typed input)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>ai.service.ts supports Claude and OpenAI via official SDKs, reads curator prompt from prompts/curator.md, accepts RawArticle[] and returns ready-made post text, compiles with strict TypeScript</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bot -> LLM API | Sending article data to external AI provider |
| LLM API -> Bot | Receiving unstructured text response |
| Config -> AI service | API key loaded from environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | ai.service.ts | mitigate | API key loaded from env var via config.ts requireEnv(), never logged; pino logger only logs model name and article count |
| T-02-06 | Tampering | LLM response | accept | LLM output is displayed as-is in Telegram; content is news curation not user-generated; prompt instructs specific format |
| T-02-07 | Denial of Service | AI API calls | mitigate | SDK clients have default timeouts; single call per pipeline run; no retry loop (retry is Phase 3 concern) |
| T-02-08 | Spoofing | AI provider | accept | Using official SDKs with HTTPS; API key authenticates requests |
</threat_model>

<verification>
1. `npx tsc --noEmit` compiles with zero errors
2. `prompts/curator.md` contains full curator prompt from rss.md
3. `src/services/ai.service.ts` exports `filterArticles` with dual-provider logic
4. No `any` types in new code
5. Both `@anthropic-ai/sdk` and `openai` in package.json dependencies
</verification>

<success_criteria>
- Curator prompt lives in an external file, editable without touching code
- AI service switches between Claude and OpenAI based on AI_MODEL env var
- filterArticles accepts typed articles and returns ready-made post text
- All code compiles under strict TypeScript with no any
</success_criteria>

<output>
After completion, create `.planning/phases/02-digest-pipeline/02-02-SUMMARY.md`
</output>

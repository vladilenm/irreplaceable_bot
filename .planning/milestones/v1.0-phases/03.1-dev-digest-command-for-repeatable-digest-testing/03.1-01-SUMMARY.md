---
phase: 03.1-dev-digest-command-for-repeatable-digest-testing
plan: 01
subsystem: telegram-bot
tags: [telegram, grammy, dev-tooling, admin, digest]

# Dependency graph
requires:
  - phase: 03-delivery-operations
    provides: runDigestPipeline, sendDigest, isAdmin helper с TTL-кэшем, idempotency guard
provides:
  - Опциональные флаги skipIdempotency/persistState в runDigestPipeline
  - Команда /dev-digest как repeatable admin-инструмент тестирования формата
affects: [04-*, future digest formatting phases]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Options-объект с optional boolean-флагами и defaults через `??` — backwards-compatible расширение сигнатуры"
    - "devRun: true маркер в pino-логах для отличия dev-запусков от production"

key-files:
  created: []
  modified:
    - src/modules/digest/digest.service.ts
    - src/bot.ts

key-decisions:
  - "Расширить runDigestPipeline через опциональный options-объект — нулевые изменения у существующих вызывающих (cron, /digest)"
  - "Dev-run публикует в реальный AI-radar тред — намеренно, чтобы видеть реальный рендер (T-03.1-05 accepted)"
  - "persistState: false гарантирует, что dev-run не портит production-идемпотентность и /status"

patterns-established:
  - "devRun: true поле в pino-логах — фильтруемый маркер dev-tool команд"
  - "Префикс 'Dev-run:' в user-facing сообщениях — мгновенная визуальная идентификация не-production прогона"

requirements-completed: [CMD-02]

# Metrics
duration: ~2min
completed: 2026-04-14
---

# Phase 03.1 Plan 01: /dev-digest command Summary

**Admin-only /dev-digest команда в bot.ts плюс опции skipIdempotency/persistState в runDigestPipeline — repeatable dev-тестирование формата без порчи state.json**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-04-14T16:33:23Z
- **Completed:** 2026-04-14T16:34:48Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- `runDigestPipeline` принимает `RunPipelineOptions` с флагами `skipIdempotency` и `persistState`; defaults сохраняют production-поведение для cron и `/digest`
- Idempotency guard и оба `writeState(...)` вызова обёрнуты условиями — dev-run не трогает `data/state.json`
- Новая команда `/dev-digest` в `src/bot.ts`: admin-gated, bypass idempotency, edit-in-place статус, вызов `sendDigest(result)`
- Каждый dev-запуск помечен `devRun: true` в структурированных логах

## Task Commits

1. **Task 1: Расширить runDigestPipeline опциональными флагами** — `c5f1e70` (feat)
2. **Task 2: Добавить /dev-digest handler в bot.ts** — `35ce38e` (feat)

## Files Created/Modified
- `src/modules/digest/digest.service.ts` — экспорт `RunPipelineOptions`, сигнатура `runDigestPipeline(opts = {})`, условные `!skipIdempotency` guard и `if (persistState) writeState(...)` обёртки, расширенный стартовый `logger.info`
- `src/bot.ts` — новый `bot.command('dev-digest', …)` handler (admin-gate → edit-in-place статус → runDigestPipeline({ skipIdempotency: true, persistState: false }) → sendDigest → финальный edit)

## Decisions Made
- Options-объект с optional boolean-полями вместо positional-аргументов — читаемо, расширяемо, обратно-совместимо
- Импорты в `bot.ts` не менялись (runDigestPipeline уже импортирован) — минимальный diff
- Тон комментариев «штурман → пилот» сохранён (`// Dev-run: skip MSK-day idempotency guard…`)

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered
None. `npx tsc --noEmit` прошёл с первого раза, grep-проверки registred новую команду и оба флага.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- `/dev-digest` готов к ручному smoke-тесту пользователем: два подряд вызова → оба публикуют; `/status` показывает прежний production last-digest; `/digest` сохраняет production-идемпотентность
- Паттерн options-объекта можно использовать для будущих дополнений `runDigestPipeline` (например, `customHoursBack`, `dryRun`)

## Self-Check: PASSED

- FOUND: src/modules/digest/digest.service.ts
- FOUND: src/bot.ts
- FOUND: commit c5f1e70
- FOUND: commit 35ce38e

---
*Phase: 03.1-dev-digest-command-for-repeatable-digest-testing*
*Completed: 2026-04-14*

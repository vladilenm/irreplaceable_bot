---
phase: 03.1-dev-digest-command-for-repeatable-digest-testing
plan: 01
type: execute
wave: 1
depends_on: [03-02]
files_modified:
  - src/bot.ts
autonomous: true
requirements: [CMD-02]
tags: [telegram, dev-tooling, admin]

must_haves:
  truths:
    - "Администратор может вызывать /dev-digest произвольное число раз в день, получая свежий собранный дайджест в AI-radar тред"
    - "/dev-digest обходит проверку isDigestPublishedToday() и запускает pipeline даже если production дайджест уже опубликован"
    - "/dev-digest не модифицирует data/state.json — последующий вызов /status и production-идемпотентность остаются корректными"
    - "Не-админ, вызвавший /dev-digest, получает отказ и pipeline не запускается"
    - "В логах виден маркер dev-run (devRun: true) — легко отличить от production-запусков"
  artifacts:
    - path: "src/bot.ts"
      provides: "/dev-digest command handler + runDigestPipeline override path that bypasses idempotency"
      contains: "bot.command('dev-digest'"
    - path: "src/modules/digest/digest.service.ts"
      provides: "runDigestPipeline with optional { skipIdempotency, persistState } options"
      contains: "skipIdempotency"
  key_links:
    - from: "src/bot.ts"
      to: "src/modules/digest/digest.service.ts"
      via: "runDigestPipeline({ skipIdempotency: true, persistState: false }) call inside /dev-digest"
      pattern: "runDigestPipeline\\(\\s*\\{"
    - from: "src/bot.ts"
      to: "src/modules/digest/digest.sender.ts"
      via: "sendDigest(result) after pipeline in /dev-digest"
      pattern: "sendDigest"
---

<objective>
Добавить команду `/dev-digest` — админский инструмент для разработки, который позволяет прогонять production-pipeline дайджеста сколько угодно раз в день для доточки формата. Бот штатно публикует результат в AI-radar тред, но не трогает `data/state.json`, чтобы не ломать production-идемпотентность и `/status`.

Purpose: пользователю нужен repeatable способ видеть реальный forматированный output дайджеста в реальном треде, без ожидания следующего дня и без ручного редактирования state.json.
Output: обновлённый `src/bot.ts` с новой командой + минимально расширенная сигнатура `runDigestPipeline` в `src/modules/digest/digest.service.ts` (опциональные флаги без breaking changes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-delivery-operations/03-02-SUMMARY.md
@.planning/phases/03-delivery-operations/03-01-SUMMARY.md

@src/bot.ts
@src/modules/digest/digest.service.ts
@src/modules/digest/digest.sender.ts
@src/config.ts
@src/utils/logger.ts

<interfaces>
<!-- Текущие контракты, на которые опирается новый handler. -->

From src/modules/digest/digest.service.ts (current):
```typescript
export interface DigestResult {
  text: string;
  itemCount: number;
  skipped: boolean;
  date: Date;
  alreadyPublished: boolean;
}

export interface PipelineState {
  lastDigestDate: string | null;
  lastSkipped: boolean;
  lastItemCount: number;
}

export function readState(): PipelineState;
export function isDigestPublishedToday(): boolean;
export async function runDigestPipeline(): Promise<DigestResult>;
```

After this plan the signature becomes:
```typescript
export interface RunPipelineOptions {
  skipIdempotency?: boolean; // default false — bypass isDigestPublishedToday short-circuit
  persistState?: boolean;    // default true  — write data/state.json after run
}
export async function runDigestPipeline(opts?: RunPipelineOptions): Promise<DigestResult>;
```
All existing callers (`src/scheduler/cron.ts`, `bot.command('digest')`) continue to work — they call `runDigestPipeline()` с defaults и получают production-поведение.

From src/modules/digest/digest.sender.ts:
```typescript
export async function sendDigest(result: DigestResult): Promise<void>;
```

From src/bot.ts (current):
```typescript
// Has isAdmin(ctx), bot.command('start'), bot.command('digest'), bot.command('status').
// Imports: runDigestPipeline, isDigestPublishedToday, readState, sendDigest, logger, config.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить runDigestPipeline опциональными флагами skipIdempotency / persistState</name>
  <files>src/modules/digest/digest.service.ts</files>
  <read_first>
    src/modules/digest/digest.service.ts
  </read_first>
  <action>
**Расширить `runDigestPipeline` в `src/modules/digest/digest.service.ts`** опциональными флагами, сохраняя обратную совместимость.

1. Добавить и экспортировать интерфейс:
```typescript
export interface RunPipelineOptions {
  /** If true, bypass isDigestPublishedToday() short-circuit. Default: false. */
  skipIdempotency?: boolean;
  /** If true, write data/state.json after the run. Default: true. */
  persistState?: boolean;
}
```

2. Изменить сигнатуру `runDigestPipeline` на:
```typescript
export async function runDigestPipeline(opts: RunPipelineOptions = {}): Promise<DigestResult> {
  const skipIdempotency = opts.skipIdempotency ?? false;
  const persistState = opts.persistState ?? true;
  // ...
}
```

3. Внутри функции:
   - Idempotency guard (блок `if (isDigestPublishedToday() && state.lastSkipped === false)`) оборачивается условием `if (!skipIdempotency && …)`. При `skipIdempotency: true` блок пропускается — pipeline продолжает работу даже если production-дайджест уже опубликован сегодня.
   - Все два `writeState(...)` вызова (в ветке "нет статей" и после успешного filterArticles) обернуть в `if (persistState) { writeState({...}) }`. При `persistState: false` файл не трогается вообще.
   - Добавить в стартовый `logger.info({ hoursBack, lastSkipped, lastDigestDate, skipIdempotency, persistState }, 'Starting digest pipeline')` два новых поля — чтобы dev-run был отличим в логах.

4. Не менять поведение существующих вызовов: `runDigestPipeline()` без аргументов = прежнее production-поведение (idempotency on, persist on). Не трогать никакие другие экспорты (`readState`, `isDigestPublishedToday`, `PipelineState`, `DigestResult`).

5. Явно: не использовать `any`. Типы опций — только boolean | undefined через optional properties.

Тон комментариев — «штурман → пилот»: факт, без восторгов. Пример: `// Dev-run: skip MSK-day idempotency guard so /dev-digest can be called repeatedly.`
  </action>
  <verify>
    <automated>cd /Users/vladilen/Documents/тнз/club-bot && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/modules/digest/digest.service.ts` экспортирует `RunPipelineOptions` с двумя опциональными boolean-полями
    - `runDigestPipeline` принимает `opts: RunPipelineOptions = {}` и внутренне использует defaults (`skipIdempotency: false`, `persistState: true`)
    - idempotency-блок пропускается если `skipIdempotency === true`
    - оба `writeState(...)` вызова происходят только если `persistState === true`
    - стартовый `logger.info` в pipeline включает поля `skipIdempotency` и `persistState`
    - никаких `any`, никаких breaking changes для вызывающих без аргументов
    - `npx tsc --noEmit` → exit 0
  </acceptance_criteria>
  <done>
    `runDigestPipeline` принимает опциональные флаги. Вызов без аргументов поведение не меняет. Вызов с `{ skipIdempotency: true, persistState: false }` прогоняет pipeline игнорируя state и не пишет state.json.
  </done>
</task>

<task type="auto">
  <name>Task 2: Добавить /dev-digest handler в bot.ts</name>
  <files>src/bot.ts</files>
  <read_first>
    src/bot.ts
    src/modules/digest/digest.service.ts
    src/modules/digest/digest.sender.ts
  </read_first>
  <action>
**Добавить обработчик `/dev-digest` в `src/bot.ts`** по паттерну существующего `/digest` (см. `bot.command('digest', …)`), с тремя ключевыми отличиями:

1. **Админ-гейт идентичен `/digest`** — переиспользуем существующий `isAdmin(ctx)` helper (с его TTL-кэшем). Non-admin получает `'Команда доступна только администраторам.'` и никакой pipeline не запускается.

2. **Idempotency pointedly bypassed** — НЕ вызывать `isDigestPublishedToday()`. Pipeline запускается всегда. Это ключевое отличие от `/digest`.

3. **State.json НЕ модифицируется** — вызов pipeline c флагами:
   ```typescript
   const result = await runDigestPipeline({ skipIdempotency: true, persistState: false });
   ```

Добавить импорт (если ещё нет — `runDigestPipeline` уже импортирован, ничего не меняем):
```typescript
// existing import line already covers it:
// import { runDigestPipeline, isDigestPublishedToday, readState } from './modules/digest/digest.service.js';
```

Расположение: после существующего `bot.command('status', …)` блока, в конце файла.

Скелет handler'а:
```typescript
// /dev-digest command -- dev-only repeatable trigger.
// Admin-gated. Bypasses isDigestPublishedToday() and does NOT persist state.json.
// Intended for format tuning during development; публикует в реальный AI-radar тред, так что
// output идентичен production-render (HTML, emoji, links).
bot.command('dev-digest', async (ctx) => {
  logger.info({ userId: ctx.from?.id, devRun: true }, '/dev-digest command received');

  if (!(await isAdmin(ctx))) {
    await ctx.reply('Команда доступна только администраторам.');
    return;
  }

  const statusMsg = await ctx.reply('Dev-run: запускаю сборку дайджеста...');

  try {
    const result = await runDigestPipeline({
      skipIdempotency: true,
      persistState: false,
    });

    if (result.skipped) {
      await ctx.api.editMessageText(
        statusMsg.chat.id,
        statusMsg.message_id,
        'Dev-run: дайджест пропущен (менее 3 значимых новостей). state.json не изменён.',
      );
      return;
    }

    await sendDigest(result);

    logger.info(
      { itemCount: result.itemCount, devRun: true },
      'Dev-digest published (state NOT persisted)',
    );

    await ctx.api.editMessageText(
      statusMsg.chat.id,
      statusMsg.message_id,
      `Dev-run: опубликовано ${result.itemCount} новостей. state.json не изменён.`,
    );
  } catch (err: unknown) {
    logger.error({ err, devRun: true }, '/dev-digest command failed');
    await ctx.api
      .editMessageText(
        statusMsg.chat.id,
        statusMsg.message_id,
        'Dev-run: ошибка при сборке дайджеста. Подробности в логах.',
      )
      .catch(() => {
        /* ignore edit failure */
      });
  }
});
```

Дополнительные правила:
- Никаких `any`. `err: unknown` — как в существующих handlers.
- Логи везде содержат `devRun: true` — для фильтрации в pino.
- Сообщения юзеру начинаются с префикса `Dev-run:` — юзер сразу видит, что это dev-запуск.
- edit-in-place паттерн идентичен `/digest` (D-15): сначала «запускаю…», потом правим результатом.
- Не менять существующие handler'ы (`/start`, `/digest`, `/status`, `isAdmin`). Ни одного касания кроме добавления нового `bot.command('dev-digest', …)`.
  </action>
  <verify>
    <automated>cd /Users/vladilen/Documents/тнз/club-bot && npx tsc --noEmit && grep -n "bot.command('dev-digest'" src/bot.ts && grep -n "skipIdempotency: true" src/bot.ts && grep -n "persistState: false" src/bot.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/bot.ts` содержит `bot.command('dev-digest'`
    - handler вызывает `isAdmin(ctx)` и отклоняет не-админов с сообщением `'Команда доступна только администраторам.'`
    - handler вызывает `runDigestPipeline({ skipIdempotency: true, persistState: false })`
    - handler НЕ вызывает `isDigestPublishedToday()`
    - handler вызывает `sendDigest(result)` в success-ветке
    - handler использует edit-in-place паттерн (`ctx.api.editMessageText` на `statusMsg`)
    - handler использует префикс `Dev-run:` в user-facing сообщениях
    - все логи в handler'е содержат поле `devRun: true`
    - `catch (err: unknown)` — никаких `any` в файле
    - `npx tsc --noEmit` → exit 0
    - существующие handler'ы `/start`, `/digest`, `/status`, helper `isAdmin` не изменены
  </acceptance_criteria>
  <done>
    Команда `/dev-digest` зарегистрирована, admin-only, обходит idempotency, не персистит state, публикует реальный отформатированный дайджест в AI-radar тред и помечена `devRun: true` в логах. TypeScript компилируется.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User command → bot handler | Любой Telegram-юзер в группе может отправить `/dev-digest` |
| bot.ts → LLM provider (ai.service) | Каждый запуск pipeline жжёт токены LLM |
| bot.ts → Telegram Admin API | Проверка админа идёт через API (закэширована 5 мин) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Spoofing | bot.ts /dev-digest | mitigate | Admin-gate через `isAdmin()` (тот же helper, что и для `/digest`/`/status`), non-admin получает отказ до любого вызова pipeline |
| T-03.1-02 | Denial of Service (LLM cost) | bot.ts /dev-digest | accept | Инструмент сознательно обходит idempotency — это его смысл. Риск злоупотребления ограничен admin-гейтом; в single-admin setup владелец клуба сам себе лимит. Cron-рейт-лимит не применяется: dev-tool. Документировано в SUMMARY и имени команды (`dev-`). |
| T-03.1-03 | Tampering | bot.ts /dev-digest → state.json | mitigate | `persistState: false` гарантирует, что dev-run не пишет в `data/state.json`, сохраняя честность production-идемпотентности и `/status` readout |
| T-03.1-04 | Repudiation | bot.ts /dev-digest | mitigate | Каждый dev-run логируется с `devRun: true` полем в pino — отличим от production в структурированных логах |
| T-03.1-05 | Information Disclosure | bot.ts /dev-digest | accept | Dev-run публикует результат в реальный AI-radar тред — намеренно, чтобы видеть реальный рендер. Считаем приемлемым: тред уже закрытый для клуба. |
</threat_model>

<verification>
1. `npx tsc --noEmit` компилируется без ошибок.
2. `grep -n "bot.command('dev-digest'" src/bot.ts` — команда зарегистрирована.
3. `grep -n "skipIdempotency" src/modules/digest/digest.service.ts` — опция добавлена в signature + использована в guard.
4. `grep -n "persistState" src/modules/digest/digest.service.ts` — опция добавлена и влияет на оба `writeState` вызова.
5. Вызов `runDigestPipeline()` без аргументов в `src/scheduler/cron.ts` и `bot.command('digest')` остаётся валидным (backwards compat) — проверяется компиляцией.
6. Manual smoke (user runs локально): два подряд `/dev-digest` → оба публикуют дайджест; сразу после этого `/status` по-прежнему показывает last digest date из последнего production-run (не затронуто dev-ранами); `/digest` всё ещё соблюдает production-идемпотентность.
</verification>

<success_criteria>
- `/dev-digest` работает как repeatable dev-триггер: admin-only, публикует реальный отформатированный дайджест в AI-radar тред произвольное число раз в день.
- `data/state.json` не меняется между dev-запусками; production `/digest` и `/status` продолжают работать корректно.
- `runDigestPipeline` получил опциональные флаги без breaking changes для cron/`/digest` вызовов.
- Строгая типизация: никаких `any`, никаких новых eslint/tsc предупреждений.
- Логи содержат `devRun: true` маркер у каждого dev-запуска.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-dev-digest-command-for-repeatable-digest-testing/03.1-01-SUMMARY.md`
</output>

---
phase: quick-260428-mn8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prompts/curator.md
autonomous: true
requirements:
  - QUICK-MN8-01  # Восстановить arrow-формат "→ https://..." в промпте под digest.formatter.ts
  - QUICK-MN8-02  # Жёсткий императив 5-7 новостей минимум, никогда не возвращать пусто
  - QUICK-MN8-03  # Привести КВОТА ПО ИСТОЧНИКАМ к новой реальности (5-7 итого)

must_haves:
  truths:
    - "Промпт curator.md в блоке ФОРМАТ ВЫХОДА генерирует строки вида '→ https://...' на отдельной строке (не markdown-link)"
    - "Промпт содержит категорическое требование 5-7 новостей всегда, никогда меньше 5, никогда пусто"
    - "Промпт содержит инструкцию НЕ писать в выводе фразы 'новостей мало' / 'недостаточно значимых'"
    - "КВОТА ПО ИСТОЧНИКАМ согласована: vc.ru 0-2 + тех-источники 4-7 = итого 5-7"
    - "В шаблоне ФОРМАТ ВЫХОДА после стрелки → стоит конкретный пример URL начинающийся с https:// (не плейсхолдер [ссылка])"
    - "Структура каждой новости: emoji+заголовок на одной строке, суть на следующих строках, → https://... на отдельной финальной строке"
  artifacts:
    - path: "prompts/curator.md"
      provides: "Curator-промпт с arrow-форматом и императивом 5-7"
      contains: "→ https://"
  key_links:
    - from: "prompts/curator.md (ФОРМАТ ВЫХОДА)"
      to: "src/modules/digest/digest.formatter.ts (isLinkLine, transformLinkLine)"
      via: "regex /^\\s*→\\s+https?:\\/\\//"
      pattern: "→ https?://"
    - from: "prompts/curator.md (ФОРМАТ ВЫХОДА)"
      to: "src/modules/digest/digest.service.ts (countDigestItems)"
      via: "regex /→ https?:\\/\\//g"
      pattern: "→ https?://"
    - from: "prompts/curator.md (ЗАДАЧА категорические правила)"
      to: "digest.service.ts skipped = itemCount < 1"
      via: "LLM всегда выдаёт ≥5 пунктов → countDigestItems ≥5 → skipped=false → sendDigest публикует"
---

<objective>
Восстановить рабочую связку «curator-промпт ↔ digest.formatter.ts ↔ digest.service.ts».

**Проблема (диагноз):**
- `digest.formatter.ts:33-35` (`isLinkLine`) регулярка: `/^\s*→\s+https?:\/\//`
- `digest.service.ts:82-85` (`countDigestItems`) регулярка: `/→ https?:\/\//g`
- Текущий `prompts/curator.md:51` задаёт формат `[emoji] *[Заголовок](ссылка)*` — markdown-link, без стрелок.
- Следствие: `countDigestItems` всегда = 0 → `skipped = itemCount < 1` всегда `true` → `sendDigest` логирует "Digest skipped, not sending" и не публикует.
- Понижение порога с 3 до 1 (предыдущий quick-fix 260428-m57) не помогло, потому что само число пунктов = 0.

**Решение:** правка ТОЛЬКО `prompts/curator.md`. Код корректен.

Purpose: Дайджест снова начнёт публиковаться. Императив 5-7 новостей гарантирует, что LLM никогда не вернёт пустоту — даже в слабые дни.
Output: Обновлённый `prompts/curator.md` с arrow-форматом и категорическими правилами по количеству.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@prompts/curator.md
@src/modules/digest/digest.formatter.ts
@src/modules/digest/digest.service.ts

<interfaces>
<!-- Контракты, которым обязан соответствовать вывод LLM. -->
<!-- Извлечено из digest.formatter.ts и digest.service.ts — править их НЕ нужно. -->

From src/modules/digest/digest.formatter.ts:
```typescript
// line 33-35: detector arrow-link строки
function isLinkLine(line: string): boolean {
  return /^\s*→\s+https?:\/\//.test(line);
}

// line 41-48: трансформация в Telegram HTML <a>
function transformLinkLine(line: string): string {
  return line.replace(
    /(→\s+)(https?:\/\/[^\s<>"]+)\s*$/,
    (_match, arrow: string, url: string) => {
      const hrefUrl = unescapeAmp(url);
      return `${arrow}<a href="${hrefUrl}">ссылка</a>`;
    },
  );
}
```

From src/modules/digest/digest.service.ts:
```typescript
// line 82-85: счётчик новостей в выводе LLM
function countDigestItems(text: string): number {
  const matches = text.match(/→ https?:\/\//g);
  return matches ? matches.length : 0;
}

// line 140: порог skip
const skipped = itemCount < 1;
```

**Вывод:** строка `→ https://реальный-url` на отдельной строке после каждой новости — единственный валидный сигнал и для счётчика, и для HTML-форматтера.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Переписать prompts/curator.md — arrow-формат + императив 5-7</name>
  <files>prompts/curator.md</files>
  <action>
Полностью перезаписать `prompts/curator.md`. Сохранить РОЛЬ, АУДИТОРИЯ, КАТЕГОРИИ, АНТИКРИТЕРИИ, ТОНАЛЬНОСТЬ как есть. Изменить четыре блока:

**1) Блок `# ЗАДАЧА` → переименовать в `# ЗАДАЧА (КАТЕГОРИЧЕСКИЕ ПРАВИЛА)` и заменить целиком:**

```
# ЗАДАЧА (КАТЕГОРИЧЕСКИЕ ПРАВИЛА)

ВСЕГДА выдавай ровно 5-7 новостей. Это обязательное требование.
Никогда не возвращай меньше 5. Никогда не возвращай пустой результат.

Если входной список новостей слабый:
- Ослабляй критерии «прорыв» и «контринтуитивность»
- Оставляй только антикритерии (явный мусор, рекламные пресс-релизы, дубли)
- Бери крепкие середняки, не выдумывай и не повторяй одно и то же
- Если за сутки нет 5 достойных — бери всё что есть, ранжируй по полезности

Никогда не пиши «новостей мало» / «недостаточно значимых» / «дайджест
сегодня тонкий» в выводе. Никаких мета-комментариев. Просто выдавай
5-7 пунктов в указанном формате.
```

**2) Блок `# КВОТА ПО ИСТОЧНИКАМ` → заменить целиком:**

```
# КВОТА ПО ИСТОЧНИКАМ
- Из vc.ru — 0-2 новости (бизнес, деньги, экономика, инвестиции
  в AI/tech). Бери если есть достойные бизнес-новости, иначе пропускай —
  добирай тех-источниками.
- Технические источники (Habr, OpenAI, Anthropic, Hugging Face,
  LangChain, Cursor, Tproger, VentureBeat, Kod) — 4-7 новостей,
  заполняем до общих 5-7.
- Итого в дайджесте всегда 5-7 новостей.
```

**3) Блок `# КРИТЕРИИ ОТБОРА (приоритет)` — оставить четыре пункта как есть, но добавить в конец блока абзац:**

```
Применяй критерии в порядке убывания. Если после применения всех
четырёх критериев у тебя меньше 5 новостей — снимай требования
с конца списка (сначала #4 контринтуитивность, потом #3 влияние,
потом #2 прорыв), но ВСЕГДА добирай до 5. Критерий #1 (практичность,
антикритерии) не снимается никогда.
```

**4) Блок `# ФОРМАТ ВЫХОДА` → заменить целиком (восстановить arrow-формат как в commit 1253fcd):**

```
# ФОРМАТ ВЫХОДА
Telegram-пост. Русский язык. Без markdown-заголовков (Telegram
не рендерит). Emoji для категорий. Краткость > развёрнутость.

Структура (строго соблюдай — каждая новость = три строки):

📡 AI-радар | [дата в формате dd.mm.yyyy]

[emoji категории] [Заголовок на русском — ёмко, 5-8 слов]
[Суть в 1-2 предложениях: что случилось + почему это важно
для человека, который строит AI-систему]
→ https://пример-полной-ссылки-из-входа.com/path

[повторить для 5-7 новостей, между блоками — одна пустая строка]

———
Дайджест Клуба Незаменимых

ВАЖНО про формат ссылки:
- Стрелка → (U+2192), пробел, затем полный URL начинающийся с
  https:// или http:// (без обёрток, без markdown, без квадратных скобок)
- URL должен быть на ОТДЕЛЬНОЙ строке после строки сути
- Никаких [текст](url), никаких *жирных* заголовков-ссылок —
  только plain-text заголовок и отдельная строка со стрелкой и URL
- Это критично для парсера дайджеста: строка вида "→ https://..."
  считается маркером новости. Без неё новость не учитывается.
```

**Важно при правке:**
- НЕ трогай блоки РОЛЬ, АУДИТОРИЯ, КАТЕГОРИИ, АНТИКРИТЕРИИ, ТОНАЛЬНОСТЬ — копируй один-в-один из текущего файла.
- Порядок блоков сохрани прежним: РОЛЬ → АУДИТОРИЯ → ЗАДАЧА → КАТЕГОРИИ → КВОТА → КРИТЕРИИ → АНТИКРИТЕРИИ → ФОРМАТ → ТОНАЛЬНОСТЬ.
- В шаблоне ФОРМАТ ВЫХОДА используй РЕАЛЬНЫЙ пример URL `https://пример-полной-ссылки-из-входа.com/path`, а НЕ плейсхолдер `[ссылка]` или `[url]` — LLM иначе может скопировать плейсхолдер дословно.
  </action>
  <verify>
    <automated>grep -c "→ https://" prompts/curator.md</automated>
    Должно вернуть ≥1 (минимум одна arrow-строка с https в шаблоне).

    Дополнительные ручные проверки (быстрые):
    - `grep -c "ВСЕГДА выдавай ровно 5-7" prompts/curator.md` → 1
    - `grep -c "Никогда не возвращай пустой результат" prompts/curator.md` → 1
    - `grep -c "5-7 новостей" prompts/curator.md` → ≥2
    - `grep -E "\\*\\[.*\\]\\(.*\\)\\*" prompts/curator.md` → пусто (старого markdown-link формата быть не должно)
    - `grep -c "новостей мало" prompts/curator.md` → 0 (это запрещённая фраза для вывода)
    - `npx tsc --noEmit` → 0 ошибок (sanity, на промпт не влияет)
  </verify>
  <done>
    `prompts/curator.md` содержит:
    1. Блок «ЗАДАЧА (КАТЕГОРИЧЕСКИЕ ПРАВИЛА)» с императивом «ВСЕГДА выдавай ровно 5-7 новостей» и запретом мета-комментариев.
    2. Блок «КВОТА ПО ИСТОЧНИКАМ» с цифрами vc.ru 0-2, тех-источники 4-7, итого 5-7.
    3. Блок «КРИТЕРИИ ОТБОРА» с пометкой о порядке снятия критериев при нехватке материала.
    4. Блок «ФОРМАТ ВЫХОДА» с arrow-форматом: emoji+заголовок / суть / `→ https://...` на отдельной строке + явное предупреждение что markdown-link `[текст](url)` запрещён.
    5. Старый формат `*[Заголовок](ссылка)*` полностью удалён.
    6. `npx tsc --noEmit` остаётся зелёным (правка не задевает TS, но проверяем что репо в норме).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify — прогнать /dev-digest и убедиться что дайджест публикуется</name>
  <what-built>
    Восстановлен arrow-формат `→ https://...` в `prompts/curator.md` под существующий
    `digest.formatter.ts` + `digest.service.ts`. Промпт теперь категорически требует
    5-7 новостей и запрещает мета-комментарии «новостей мало».
  </what-built>
  <how-to-verify>
    Прогнать дев-команду дайджеста и убедиться, что публикация проходит:

    1. Локально или на VPS выполнить `/dev-digest` (или эквивалентный триггер из проекта,
       обходящий MSK-идемпотентность через `skipIdempotency: true`).
    2. Проверить логи `pino`:
       - Должна быть строка `Digest ready` с `itemCount` от 5 до 7.
       - НЕ должно быть `No items in digest, marking as skipped`.
       - Должна быть финальная `Digest published` с `itemCount` 5-7.
    3. Открыть тред «AI-радар» в Telegram-группе клуба:
       - В сообщении 5-7 новостей.
       - Каждая новость: жирный заголовок с emoji категории, 1-2 строки сути,
         кликабельная «ссылка» в конце (HTML `<a href="...">ссылка</a>`).
       - Заголовки больше НЕ являются ссылками (markdown-link убран).
    4. Если LLM вернула 4 или меньше новостей — это значит промпт всё ещё мягкий,
       вернуться к Task 1 и усилить категорические правила (например, добавить
       требование «не возвращай ответ короче 5 пунктов под угрозой невалидного вывода»).
    5. Если 5-7 новостей пришли, но `skipped: true` в логах — значит формат стрелки
       где-то сломан, проверить вывод LLM глазами и сравнить с regex `/→ https?:\/\//g`.
  </how-to-verify>
  <resume-signal>Type "approved" если дайджест опубликован с 5-7 новостями, или опиши что пошло не так (count, skipped, формат)</resume-signal>
</task>

</tasks>

<verification>
- `grep "→ https://" prompts/curator.md` находит arrow-формат в шаблоне.
- `grep -E "\*\[.*\]\(.*\)\*" prompts/curator.md` ничего не находит (старый markdown-link удалён).
- `npx tsc --noEmit` зелёный.
- Реальный прогон `/dev-digest` публикует пост с 5-7 новостями в тред AI-радара.
- В логах `itemCount` ∈ [5,7], `skipped: false`, `Digest published`.
</verification>

<success_criteria>
1. `prompts/curator.md` соответствует контрактам `digest.formatter.ts` (regex `/^\s*→\s+https?:\/\//`) и `digest.service.ts` (regex `/→ https?:\/\//g`).
2. Блок ЗАДАЧА категорически требует 5-7 новостей и запрещает мета-комментарии о слабом дне.
3. Дев-прогон `/dev-digest` приводит к успешной публикации в Telegram-тред AI-радара.
4. Изменения ограничены ровно одним файлом — `prompts/curator.md`. Никаких правок в `src/`.
</success_criteria>

<output>
After completion, create `.planning/quick/260428-mn8-https-prompts-curator-md-formatter-5-7/260428-mn8-SUMMARY.md` documenting:
- Diagnosis (markdown-link vs arrow-format mismatch with formatter/service regex)
- Exact changes to prompts/curator.md (4 blocks rewritten)
- Verification result from `/dev-digest` (itemCount, skipped, link to published digest)
</output>

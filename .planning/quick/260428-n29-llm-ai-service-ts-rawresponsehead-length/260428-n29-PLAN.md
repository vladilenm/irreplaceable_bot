---
phase: quick-260428-n29
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/ai.service.ts
autonomous: true
requirements:
  - QUICK-260428-n29-DIAG-LOG
must_haves:
  truths:
    - "When filterArticles runs, the runtime logs an info-level entry containing the first 800 chars of the LLM raw response and its full length"
    - "The new diagnostic log fires AFTER `result` is assigned (both Claude and OpenAI branches) and BEFORE the existing 'AI filtering complete' log and the return"
    - "Existing 'AI filtering complete' log, filterArticles signature, return type, and curator prompt remain unchanged"
    - "TypeScript strict build (tsc) still passes with no `any` introduced"
  artifacts:
    - path: "src/services/ai.service.ts"
      provides: "AI raw response (debug) info-level log emitted before filtering-complete log"
      contains: "AI raw response (debug)"
  key_links:
    - from: "src/services/ai.service.ts (post-branch result assignment)"
      to: "logger.info call with rawResponseHead/rawResponseLength/model fields"
      via: "pino logger import already in file"
      pattern: "rawResponseHead.*result\\.slice\\(0, 800\\)"
---

<objective>
Add a diagnostic info-level log of the LLM raw response (first 800 chars + full length) inside `filterArticles` in `src/services/ai.service.ts`, so we can inspect actual model output in production without flipping LOG_LEVEL=debug.

Purpose: Curator/digest pipeline currently logs only metadata ("AI filtering complete"); when downstream parsing or formatting misbehaves, we have no record of what the LLM actually returned. A bounded head + length lets us diagnose without dumping multi-KB payloads.
Output: One additional `logger.info(...)` call in `src/services/ai.service.ts`, no other changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@src/services/ai.service.ts
@src/utils/logger.ts

<interfaces>
<!-- Already in src/services/ai.service.ts — no new types needed. -->
<!-- pino logger from src/utils/logger.ts: logger.info(obj, msg) — camelCase fields (project convention, see line 65-68). -->
<!-- `result: string` is assigned in both Claude (line 47) and OpenAI (line 62) branches BEFORE current `logger.info` at line 65. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add raw-response diagnostic log in filterArticles</name>
  <files>src/services/ai.service.ts</files>
  <action>
In `src/services/ai.service.ts`, locate the existing `logger.info({ model: config.aiModel, inputArticles: articles.length }, 'AI filtering complete')` call (currently lines 65-68). IMMEDIATELY BEFORE that call (i.e. AFTER the if/else block where `result` is assigned in both Claude and OpenAI branches, BEFORE the existing filtering-complete log, and BEFORE `return result`), insert a new info-level log:

```ts
logger.info(
  {
    rawResponseHead: result.slice(0, 800),
    rawResponseLength: result.length,
    model: config.aiModel,
  },
  'AI raw response (debug)',
);
```

Constraints:
- Field names MUST be camelCase (`rawResponseHead`, `rawResponseLength`) — matches existing pino convention in the file (e.g. `inputArticles`).
- Level MUST be `info` (NOT `debug`) so the entry appears under the production default log level without forcing LOG_LEVEL=debug.
- Slice length is exactly 800 — do not parameterise, do not introduce config knobs.
- The message string is `'AI raw response (debug)'` — English to match the existing 'AI filtering complete' log style (CLAUDE.md allows English log messages even on Russian project).
- Do NOT change the system prompt, the `filterArticles` signature/contract, the return value, the curator prompt loading, or any other behaviour.
- Do NOT introduce new types, new interfaces, or any `any` — strict TypeScript stays strict.
- Do NOT touch the existing 'AI filtering complete' log; the new log must appear immediately above it.
  </action>
  <verify>
    <automated>cd /Users/vladilen/Documents/тнз/club-bot && npx tsc --noEmit && grep -n "AI raw response (debug)" src/services/ai.service.ts && grep -n "rawResponseHead: result.slice(0, 800)" src/services/ai.service.ts && grep -n "rawResponseLength: result.length" src/services/ai.service.ts && grep -c "AI filtering complete" src/services/ai.service.ts</automated>
  </verify>
  <done>
- `npx tsc --noEmit` exits 0 (strict build clean, no `any` introduced).
- `grep "AI raw response (debug)"` matches exactly one line in `src/services/ai.service.ts`.
- `grep "rawResponseHead: result.slice(0, 800)"` matches exactly one line.
- `grep "rawResponseLength: result.length"` matches exactly one line.
- `grep -c "AI filtering complete"` returns `1` (existing log preserved, not duplicated or removed).
- Visual inspection: new `logger.info` block sits AFTER the if/else `result` assignment and IMMEDIATELY BEFORE the existing 'AI filtering complete' `logger.info`.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (strict TS, no `any`).
- Only `src/services/ai.service.ts` changed — `git diff --stat` shows a single file with one hunk added (a 7-line `logger.info` block).
- New log fires for both Claude and OpenAI code paths because it sits AFTER the branch where `result` is assigned in both branches.
</verification>

<success_criteria>
- Single `logger.info(... 'AI raw response (debug)')` call added in `filterArticles`, located between the if/else `result` assignment and the existing 'AI filtering complete' log.
- Fields exactly: `rawResponseHead` (first 800 chars of `result`), `rawResponseLength` (full `result.length`), `model` (`config.aiModel`).
- No changes to filterArticles signature, return value, curator prompt, system prompt, types, or any other file.
- TypeScript strict build remains green.
</success_criteria>

<output>
After completion, no SUMMARY.md is required for a quick task. The git commit message produced by the execute step is sufficient documentation.
</output>

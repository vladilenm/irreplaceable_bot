---
phase: quick-260428-m57
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/digest/digest.service.ts
  - prompts/curator.md
  - src/bot.ts
autonomous: true
requirements:
  - QUICK-260428-M57

must_haves:
  truths:
    - "Дайджест публикуется, когда LLM вернула хотя бы 1 новость (порог снижен с 3 до 1)"
    - "prompts/curator.md просит 3-5 новостей с гибкой квотой vc.ru 0-2 и ослаблением критериев в слабые дни"
    - "Сообщения /digest и /dev-digest при пропуске говорят правду: пропуск только когда ноль новостей"
    - "Поведение pipeline в случае 0 статей или 0 итемов от LLM не сломано — флаг skipped по-прежнему выставляется"
  artifacts:
    - path: "src/modules/digest/digest.service.ts"
      provides: "Pipeline с порогом itemCount < 1"
      contains: "itemCount < 1"
    - path: "prompts/curator.md"
      provides: "Смягчённый промпт куратора с диапазоном 3-5 и vc.ru 0-2"
      contains: "3-5"
    - path: "src/bot.ts"
      provides: "Обновлённые сообщения о пропуске для /digest и /dev-digest"
      contains: "ни одной новости не прошло фильтр"
  key_links:
    - from: "src/modules/digest/digest.service.ts:140"
      to: "src/bot.ts:88 + src/bot.ts:187"
      via: "result.skipped → reply text"
      pattern: "result\\.skipped"
---

<objective>
Снизить порог публикации дайджеста с 3 до 1 и смягчить prompts/curator.md (3-5 новостей, гибкая квота vc.ru 0-2, ослабление критериев в слабые дни). Цель: дайджест должен выходить каждый день, даже когда новостной поток слабый, а не пропускаться.

Purpose: Привычка ежедневного захода в клуб критична для core value MVP — пропуск дайджеста ломает её. При слабом дне 1-2 новости лучше, чем 0.
Output: Обновлённый порог в digest.service.ts, смягчённый prompts/curator.md, синхронизированные сообщения о пропуске в bot.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

@src/modules/digest/digest.service.ts
@prompts/curator.md
@src/bot.ts

<interfaces>
<!-- Релевантные строки из текущего кода (для исполнителя — без скаута по репо). -->

src/modules/digest/digest.service.ts:138-149 (текущий блок порога):
```ts
const text = await filterArticles(articles);
const itemCount = countDigestItems(text);
const skipped = itemCount < 3;

if (skipped) {
  logger.warn(
    { itemCount },
    'Fewer than 3 items in digest, marking as skipped',
  );
} else {
  logger.info({ itemCount }, 'Digest ready');
}
```

src/bot.ts:84-91 (хендлер /digest, ветка skipped):
```ts
if (result.skipped) {
  await ctx.api.editMessageText(
    statusMsg.chat.id,
    statusMsg.message_id,
    'Дайджест пропущен: недостаточно значимых новостей (менее 3).',
  );
  return;
}
```

src/bot.ts:183-190 (хендлер /dev-digest, ветка skipped):
```ts
if (result.skipped) {
  await ctx.api.editMessageText(
    statusMsg.chat.id,
    statusMsg.message_id,
    'Dev-run: дайджест пропущен (менее 3 значимых новостей). state.json не изменён.',
  );
  return;
}
```

Тестовых файлов с захардкоженным порогом нет (grep по `tests/` и `*.test.ts` пуст). Других вхождений «менее 3 / < 3 / Fewer than 3» в коде нет (verified grep).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Снизить порог skipped с 3 до 1 в digest.service.ts</name>
  <files>src/modules/digest/digest.service.ts</files>
  <action>
В файле `src/modules/digest/digest.service.ts` на строке 140 заменить:
- `const skipped = itemCount < 3;` → `const skipped = itemCount < 1;`

И обновить warning-лог (строки 142-146), чтобы сообщение отражало новый смысл (пропуск только когда LLM не вернула ни одного итема):
- Было: `'Fewer than 3 items in digest, marking as skipped'`
- Стало: `'No items in digest, marking as skipped'`

Не трогать остальную логику (idempotency guard, 48h fallback при `lastSkipped`, `articles.length === 0` на строке 121 — там skipped корректно выставляется отдельной веткой и должен остаться). Тип возврата, сигнатура, поля state.json — без изменений.

Никаких новых env-флагов или config-переменных не вводить (constraint: прямые правки, simplicity).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -n "itemCount < 1" src/modules/digest/digest.service.ts && grep -n "No items in digest" src/modules/digest/digest.service.ts && ! grep -n "itemCount < 3\|Fewer than 3" src/modules/digest/digest.service.ts</automated>
  </verify>
  <done>
- `itemCount < 1` присутствует на строке ~140
- warning-сообщение `'No items in digest, marking as skipped'` присутствует
- старых строк `itemCount < 3` и `Fewer than 3` в файле нет
- `tsc --noEmit` проходит без ошибок
  </done>
</task>

<task type="auto">
  <name>Task 2: Смягчить prompts/curator.md (3-5 новостей, vc.ru 0-2, ослабление в слабые дни)</name>
  <files>prompts/curator.md</files>
  <action>
В файле `prompts/curator.md` сделать три точечные правки. Все остальные секции (РОЛЬ, АУДИТОРИЯ, КАТЕГОРИИ, КРИТЕРИИ ОТБОРА, АНТИКРИТЕРИИ, ФОРМАТ ВЫХОДА, ТОНАЛЬНОСТЬ) остаются без изменений.

**1. Секция «# ЗАДАЧА» (строки 10-12).** Заменить:
```
# ЗАДАЧА
Из предоставленного списка статей (заголовок + описание + ссылка)
выбери 5-7 самых значимых за последние сутки.
```
на:
```
# ЗАДАЧА
Из предоставленного списка статей (заголовок + описание + ссылка)
выбери 3-5 самых значимых за последние сутки. Если день слабый —
бери что есть (минимум 1), ослабляй критерии прорыва и
контринтуитивности, оставляй только антикритерии.
```

**2. Секция «# КВОТА ПО ИСТОЧНИКАМ» (строки 22-27).** Заменить:
```
# КВОТА ПО ИСТОЧНИКАМ
- Из vc.ru — ровно 2 новости в каждом дайджесте (бизнес, деньги,
  экономика, инвестиции в AI/tech). Если за сутки на vc.ru нет
  достойных бизнес-новостей — бери 1, но не 0.
- Остальные 3-5 новости — из технических источников (Habr, OpenAI,
  Anthropic, Hugging Face, LangChain, Cursor, Tproger, VentureBeat, Kod).
```
на:
```
# КВОТА ПО ИСТОЧНИКАМ
- Из vc.ru — 0-2 новости (бизнес, деньги, экономика, инвестиции
  в AI/tech). Бери если есть достойные бизнес-новости, иначе пропускай.
- Технические источники (Habr, OpenAI, Anthropic, Hugging Face,
  LangChain, Cursor, Tproger, VentureBeat, Kod) — 2-5 новостей.
- Итого в дайджесте 3-5 новостей.
```

**3. Секция «# ФОРМАТ ВЫХОДА» (строка 53).** Заменить:
```
[повторить для 5-7 новостей]
```
на:
```
[повторить для 3-5 новостей]
```

Тон ("штурман → пилот", прямой) и структуру шапки/футера не менять. Русский язык сохранить.
  </action>
  <verify>
    <automated>grep -n "3-5 самых значимых" prompts/curator.md && grep -n "Если день слабый" prompts/curator.md && grep -n "0-2 новости" prompts/curator.md && grep -n "Итого в дайджесте 3-5" prompts/curator.md && grep -n "повторить для 3-5 новостей" prompts/curator.md && ! grep -n "5-7 самых значимых\|ровно 2 новости\|повторить для 5-7" prompts/curator.md</automated>
  </verify>
  <done>
- В «# ЗАДАЧА» стоит «3-5 самых значимых» + клауза «Если день слабый — бери что есть (минимум 1), ослабляй критерии прорыва и контринтуитивности»
- В «# КВОТА ПО ИСТОЧНИКАМ» vc.ru — «0-2 новости», тех-источники — «2-5 новостей», итог — «3-5»
- В «# ФОРМАТ ВЫХОДА» строка повторения — «3-5 новостей»
- Старых формулировок «5-7 самых значимых», «ровно 2 новости», «повторить для 5-7» в файле нет
  </done>
</task>

<task type="auto">
  <name>Task 3: Синхронизировать сообщения о пропуске в /digest и /dev-digest</name>
  <files>src/bot.ts</files>
  <action>
В файле `src/bot.ts` обновить два сообщения, чтобы они правдиво отражали новый порог (пропуск только когда LLM не вернула ни одной новости).

**1. Хендлер /digest (строка 88).** Заменить:
- Было: `'Дайджест пропущен: недостаточно значимых новостей (менее 3).'`
- Стало: `'Дайджест пропущен: ни одной новости не прошло фильтр.'`

**2. Хендлер /dev-digest (строка 187).** Заменить:
- Было: `'Dev-run: дайджест пропущен (менее 3 значимых новостей). state.json не изменён.'`
- Стало: `'Dev-run: дайджест пропущен: ни одной новости не прошло фильтр. state.json не изменён.'`

Структуру вызовов `ctx.api.editMessageText`, обработку ошибок, остальные ветки (успешная публикация) и логику не трогать. Никаких новых функций или хелперов — только тексты строк.

Перед коммитом убедиться через grep, что других вхождений «менее 3» / «недостаточно значимых» в репозитории не осталось.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -n "ни одной новости не прошло фильтр" src/bot.ts | wc -l | grep -q "^[[:space:]]*2$" && ! grep -rn "менее 3\|недостаточно значимых" src/ prompts/</automated>
  </verify>
  <done>
- В `src/bot.ts` ровно 2 строки содержат «ни одной новости не прошло фильтр» (для /digest и /dev-digest)
- Старые формулировки «менее 3» и «недостаточно значимых» полностью удалены из `src/` и `prompts/`
- `tsc --noEmit` проходит без ошибок
- Сборка не сломана, остальная логика хендлеров не затронута
  </done>
</task>

</tasks>

<verification>
После всех трёх задач выполнить smoke-проверки:

1. **TypeScript:** `npx tsc --noEmit` — проходит чисто.
2. **Грепы синхронизации:**
   - `grep -rn "менее 3\|< 3\|Fewer than 3\|5-7 самых значимых\|ровно 2 новости" src/ prompts/` — должен вернуть 0 результатов.
   - `grep -n "itemCount < 1" src/modules/digest/digest.service.ts` — 1 результат.
   - `grep -n "ни одной новости не прошло фильтр" src/bot.ts` — 2 результата.
3. **Лог-семантика:** в `digest.service.ts` warning теперь говорит «No items in digest…», что согласуется с новым условием `itemCount < 1`.
4. **Промпт-семантика:** `prompts/curator.md` теперь просит 3-5 новостей с диапазоном vc.ru 0-2 и инструкцией ослаблять критерии в слабые дни.
</verification>

<success_criteria>
- Порог `skipped` снижен до `itemCount < 1` — дайджест публикуется при наличии хотя бы 1 новости от LLM.
- `prompts/curator.md` запрашивает 3-5 новостей, vc.ru квота 0-2, в слабые дни модель ослабляет критерии прорыва/контринтуитивности (только антикритерии остаются жёсткими).
- Оба сообщения о пропуске (`/digest`, `/dev-digest`) правдивы для нового порога.
- TypeScript строгая компиляция чистая, новых env-флагов/config не введено, остальная логика pipeline (idempotency guard, 48h fallback, persistState) не затронута.
- Никаких legacy-формулировок «менее 3» / «5-7 самых значимых» / «ровно 2 новости» в `src/` и `prompts/`.
</success_criteria>

<output>
After completion, create `.planning/quick/260428-m57-3-1-prompts-curator-md-3-5-vc-ru-0-2/260428-m57-SUMMARY.md` summarizing:
- Files changed (digest.service.ts, prompts/curator.md, bot.ts)
- Old vs new threshold (3 → 1)
- Old vs new prompt ranges (5-7 → 3-5, vc.ru 2 → 0-2)
- Verification commands run and their results
</output>

---
phase: quick-260428-m57
plan: 01
subsystem: digest
tags: [digest, prompt, threshold, ux]
key-files:
  modified:
    - src/modules/digest/digest.service.ts
    - prompts/curator.md
    - src/bot.ts
decisions:
  - Порог skipped снижен до itemCount < 1 — дайджест публикуется при наличии >=1 новости
  - vc.ru квота сделана гибкой 0-2 (было «ровно 2») — пропускаем источник в слабые дни
  - Сообщения о пропуске синхронизированы с новым смыслом (skip только при 0 итемов)
metrics:
  tasks_completed: 3
  duration: ~3min
  files_changed: 3
  commits: 3
  completed: 2026-04-28
---

# Quick 260428-m57: Lower digest threshold + soften curator prompt — Summary

**One-liner:** Снижен порог публикации дайджеста с 3 до 1 итема, prompts/curator.md
переделан на 3-5 новостей с гибкой квотой vc.ru 0-2 и ослаблением критериев в слабые дни,
сообщения о пропуске в /digest и /dev-digest приведены в соответствие с новым порогом.

## Files Changed

| File | Change | Commit |
|------|--------|--------|
| `src/modules/digest/digest.service.ts` | `itemCount < 3` → `itemCount < 1`; warn-лог `'Fewer than 3 items in digest…'` → `'No items in digest…'` | `5735802` |
| `prompts/curator.md` | ЗАДАЧА: 5-7 → 3-5 + клауза о слабом дне; КВОТА: vc.ru «ровно 2» → «0-2», тех-источники «3-5» → «2-5», добавлена строка «Итого 3-5»; ФОРМАТ: «повторить для 5-7» → «повторить для 3-5» | `85184de` |
| `src/bot.ts` | /digest skip text: «менее 3» → «ни одной новости не прошло фильтр»; /dev-digest skip text синхронизирован, «state.json не изменён» сохранено | `4ee9e7e` |

## Old vs New

### Threshold (digest.service.ts:140)

| Было | Стало |
|------|-------|
| `const skipped = itemCount < 3;` | `const skipped = itemCount < 1;` |
| warn: `'Fewer than 3 items in digest, marking as skipped'` | warn: `'No items in digest, marking as skipped'` |

### Prompt ranges (curator.md)

| Секция | Было | Стало |
|--------|------|-------|
| ЗАДАЧА | «выбери 5-7 самых значимых» | «выбери 3-5 самых значимых» + «Если день слабый — бери что есть (минимум 1), ослабляй критерии прорыва и контринтуитивности» |
| КВОТА vc.ru | «ровно 2 новости… если нет — бери 1, но не 0» | «0-2 новости… бери если есть достойные, иначе пропускай» |
| КВОТА тех-источники | «Остальные 3-5 новости» | «2-5 новостей» (+ «Kod» уже был в списке) |
| КВОТА итог | (отсутствовало) | «Итого в дайджесте 3-5 новостей» |
| ФОРМАТ ВЫХОДА | «повторить для 5-7 новостей» | «повторить для 3-5 новостей» |

### Skip messages (bot.ts)

| Хендлер | Было | Стало |
|---------|------|-------|
| /digest (l.88) | `'Дайджест пропущен: недостаточно значимых новостей (менее 3).'` | `'Дайджест пропущен: ни одной новости не прошло фильтр.'` |
| /dev-digest (l.187) | `'Dev-run: дайджест пропущен (менее 3 значимых новостей). state.json не изменён.'` | `'Dev-run: дайджест пропущен: ни одной новости не прошло фильтр. state.json не изменён.'` |

## Verification Run

| Command | Expected | Result |
|---------|----------|--------|
| `npx tsc --noEmit` | clean | clean (no output) |
| `grep -c "itemCount < 1" src/modules/digest/digest.service.ts` | `1` | `1` |
| `grep -c "ни одной новости не прошло фильтр" src/bot.ts` | `2` | `2` |
| `grep -rn "менее 3\|< 3\|Fewer than 3\|5-7 самых значимых\|ровно 2 новости" src/ prompts/` | 0 results | 0 results (clean) |
| `grep -n "3-5 самых значимых" prompts/curator.md` | 1 hit | hit on line 12 |
| `grep -n "0-2 новости" prompts/curator.md` | 1 hit | hit on line 25 |
| `grep -n "Итого в дайджесте 3-5" prompts/curator.md` | 1 hit | hit on line 29 |
| `grep -n "повторить для 3-5 новостей" prompts/curator.md` | 1 hit | hit on line 55 |

## Deviations from Plan

None — plan executed exactly as written. Все три задачи применены точечно, остальная логика
pipeline (idempotency guard, 48h fallback при `lastSkipped`, ветка `articles.length === 0`,
структура `DigestResult`/`PipelineState`, persistState) не затронута.

## Self-Check: PASSED

**Files exist:**
- `src/modules/digest/digest.service.ts` — FOUND
- `prompts/curator.md` — FOUND
- `src/bot.ts` — FOUND

**Commits exist:**
- `5735802` (Task 1) — FOUND in `git log --oneline -5`
- `85184de` (Task 2) — FOUND in `git log --oneline -5`
- `4ee9e7e` (Task 3) — FOUND in `git log --oneline -5`

**Success criteria:**
- [x] Порог `skipped` снижен до `itemCount < 1`
- [x] `prompts/curator.md` запрашивает 3-5 новостей, vc.ru 0-2, ослабление в слабые дни
- [x] Оба сообщения о пропуске правдивы для нового порога
- [x] TypeScript строгая компиляция чистая, никаких новых env/config
- [x] Никаких legacy-формулировок в `src/` и `prompts/`

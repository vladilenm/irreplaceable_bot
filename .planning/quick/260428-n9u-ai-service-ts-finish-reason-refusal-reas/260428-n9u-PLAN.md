---
phase: quick-260428-n9u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/ai.service.ts
autonomous: true
requirements:
  - QUICK-260428-n9u
must_haves:
  truths:
    - "Anthropic branch logs stop_reason and usage at info level on every LLM call"
    - "OpenAI-compat branch logs finish_reason, refusal, reasoning_content (truncated 400), tool_calls count, usage and serialized choice (truncated 1500) at info level on every LLM call"
    - "max_tokens is 4000 in both Anthropic and OpenAI-compat branches"
    - "Old 'AI raw response (debug)' log (rawResponseHead/Length) is removed — superseded by per-branch debug logs"
    - "TypeScript compiles with --noEmit (no any usage; refusal/reasoning_content accessed via typed assertion `as { ... }`)"
  artifacts:
    - path: "src/services/ai.service.ts"
      provides: "Per-branch LLM debug logging + raised max_tokens"
      contains: "Anthropic response (debug)"
    - path: "src/services/ai.service.ts"
      provides: "OpenAI-compat per-choice debug logging"
      contains: "OpenAI response (debug)"
  key_links:
    - from: "src/services/ai.service.ts (Anthropic branch)"
      to: "logger.info Anthropic response (debug)"
      via: "logger call inserted between client.messages.create return and content[0] parse"
      pattern: "Anthropic response \\(debug\\)"
    - from: "src/services/ai.service.ts (OpenAI branch)"
      to: "logger.info OpenAI response (debug)"
      via: "logger call inserted between client.chat.completions.create return and content extraction"
      pattern: "OpenAI response \\(debug\\)"
---

<objective>
Расширить debug-инструментацию LLM-вызовов в `ai.service.ts`, чтобы при пустом/обрезанном ответе DeepSeek/Claude диагностические данные (finish_reason, refusal, reasoning_content, usage, stop_reason) попадали в логи без повторного запуска. Дополнительно поднять `max_tokens` 2000 → 4000 в обеих ветках, чтобы устранить вероятный truncation. Удалить устаревший `AI raw response (debug)` лог из quick-260428-n29 — две новые per-branch строки покрывают всё, что он давал, и больше.

Purpose: Текущий лог из n29 показывает только `result.slice(0, 800)` после ветвления — он не различает причины пустого ответа (refusal, max_tokens cutoff, reasoning без content, tool_calls). Per-branch логирование raw-фрагментов выдачи провайдера сразу после `create(...)` даёт явный сигнал для следующей итерации диагностики, не дожидаясь нового пустого прогона.

Output: Один атомарный коммит, изменяющий `src/services/ai.service.ts`. Никаких новых файлов, никаких изменений интерфейсов/контрактов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/services/ai.service.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

Current `src/services/ai.service.ts` (full file, 80 lines):
```ts
import Anthropic from '@anthropic-ai/sdk';
import OpenAI from 'openai';
import { readFileSync } from 'node:fs';
import { config } from '../config.js';
import { logger } from '../utils/logger.js';
import type { RawArticle } from '../types/index.js';

const curatorPrompt = readFileSync(
  new URL('../../prompts/curator.md', import.meta.url),
  'utf-8',
);

function formatArticlesForLLM(articles: RawArticle[]): string { /* ... */ }
function isClaude(model: string): boolean { return model.startsWith('claude'); }

export async function filterArticles(articles: RawArticle[]): Promise<string> {
  const formattedArticles = formatArticlesForLLM(articles);
  const userMessage = `...${formattedArticles}`;

  let result: string;

  if (isClaude(config.aiModel)) {
    const client = new Anthropic({ apiKey: config.aiApiKey });
    const response = await client.messages.create({
      model: config.aiModel,
      max_tokens: 2000,                       // ← change to 4000 (line ~38)
      system: curatorPrompt,
      messages: [{ role: 'user', content: userMessage }],
    });
    // ← INSERT Anthropic debug log HERE (after create, before firstBlock parse)

    const firstBlock = response.content[0];
    if (!firstBlock || firstBlock.type !== 'text') {
      throw new Error('Unexpected Anthropic response: no text block');
    }
    result = firstBlock.text;
  } else {
    const client = new OpenAI({
      apiKey: config.aiApiKey,
      ...(config.aiBaseUrl ? { baseURL: config.aiBaseUrl } : {}),
    });
    const response = await client.chat.completions.create({
      model: config.aiModel,
      max_tokens: 2000,                       // ← change to 4000 (line ~55)
      messages: [
        { role: 'system', content: curatorPrompt },
        { role: 'user', content: userMessage },
      ],
    });
    // ← INSERT OpenAI debug log HERE (after create, before content extraction)

    result = response.choices[0]?.message?.content ?? '';
  }

  // ← REMOVE this entire block (lines 65-72, n29 legacy):
  logger.info(
    {
      rawResponseHead: result.slice(0, 800),
      rawResponseLength: result.length,
      model: config.aiModel,
    },
    'AI raw response (debug)',
  );

  logger.info(                                 // ← KEEP unchanged
    { model: config.aiModel, inputArticles: articles.length },
    'AI filtering complete',
  );

  return result;
}
```

Anthropic SDK response shape (from @anthropic-ai/sdk types — already imported, no new types needed):
```ts
// response: Anthropic.Messages.Message
response.stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use' | null
response.usage: { input_tokens: number; output_tokens: number; cache_creation_input_tokens?: number | null; cache_read_input_tokens?: number | null }
```

OpenAI SDK response shape:
```ts
// response: OpenAI.Chat.Completions.ChatCompletion
response.choices[0].finish_reason: 'stop' | 'length' | 'tool_calls' | 'content_filter' | 'function_call'
response.choices[0].message.tool_calls?: Array<...> | undefined
response.usage: { prompt_tokens, completion_tokens, total_tokens } | undefined
// NON-STANDARD fields (DeepSeek/OpenRouter extensions, NOT in OpenAI SDK types):
//   choice.message.refusal: string | null         ← needs `as { refusal?: string | null }`
//   choice.message.reasoning_content: string      ← needs `as { reasoning_content?: string }`
```

CLAUDE.md project rules (load-bearing for this task):
- «Типизация: строгий TypeScript, никаких `any`» — use `as { refusal?: string | null }` style assertions, NOT `as any`.
- Logger is pino (existing). `logger.info({ ...obj }, 'message')` is the established signature in this file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch ai.service.ts — per-branch debug logs + max_tokens 4000</name>
  <files>src/services/ai.service.ts</files>
  <action>
Make four edits in a single Edit-tool pass (or one Write of the full file):

(1) Anthropic branch — change `max_tokens: 2000` → `max_tokens: 4000` (line ~38).

(2) Anthropic branch — insert AFTER the closing `})` of `client.messages.create({...})` and BEFORE `const firstBlock = response.content[0];`:
```ts
logger.info(
  {
    model: config.aiModel,
    stopReason: response.stop_reason,
    usage: response.usage,
  },
  'Anthropic response (debug)',
);
```

(3) OpenAI-compat branch — change `max_tokens: 2000` → `max_tokens: 4000` (line ~55).

(4) OpenAI-compat branch — insert AFTER the closing `})` of `client.chat.completions.create({...})` and BEFORE `result = response.choices[0]?.message?.content ?? '';`:
```ts
const choice = response.choices[0];
logger.info(
  {
    model: config.aiModel,
    finishReason: choice?.finish_reason ?? null,
    refusal:
      (choice?.message as { refusal?: string | null } | undefined)
        ?.refusal ?? null,
    reasoningContent:
      (choice?.message as { reasoning_content?: string } | undefined)
        ?.reasoning_content?.slice(0, 400) ?? null,
    toolCallsCount: choice?.message?.tool_calls?.length ?? 0,
    usage: response.usage,
    choiceJson: JSON.stringify(choice ?? null).slice(0, 1500),
  },
  'OpenAI response (debug)',
);
```
Then update the existing `result = response.choices[0]?.message?.content ?? '';` to reuse the local `choice` for consistency: `result = choice?.message?.content ?? '';`.

(5) DELETE the entire block (lines 65-72 in current file) that logs `'AI raw response (debug)'` with `rawResponseHead`/`rawResponseLength`. The two new per-branch logs supersede it (covering both branches with provider-specific fields, not just truncated content).

(6) Leave `'AI filtering complete'` log (current lines 74-77) UNTOUCHED.

Constraints:
- NO `any` anywhere. The non-standard DeepSeek fields (`refusal`, `reasoning_content`) MUST be accessed via `as { refusal?: string | null } | undefined` and `as { reasoning_content?: string } | undefined` — explicit shapes, optional, nullable where applicable.
- No new imports needed (`logger`, `Anthropic`, `OpenAI` already imported).
- Log message strings stay in English (matches existing `'AI filtering complete'`, `'AI raw response (debug)'` style).
- Do not refactor `filterArticles` signature, do not extract helpers, do not change return type.
  </action>
  <verify>
    <automated>cd /Users/vladilen/Documents/тнз/club-bot && npx tsc --noEmit</automated>
  </verify>
  <done>
- `src/services/ai.service.ts` contains exactly two new `logger.info` calls: `'Anthropic response (debug)'` and `'OpenAI response (debug)'`.
- Old `'AI raw response (debug)'` block is gone.
- Both `max_tokens: 2000` occurrences are replaced with `max_tokens: 4000`.
- `npx tsc --noEmit` exits 0 with no diagnostics.
- `grep -n "any" src/services/ai.service.ts` returns no new `: any` or `as any` usage (pre-existing matches in unrelated identifiers like `Anthropic` are fine; the regex check is on `\\bany\\b` keyword usage in type position).
- `grep -c "max_tokens: 4000" src/services/ai.service.ts` returns `2`.
- `grep -c "max_tokens: 2000" src/services/ai.service.ts` returns `0`.
- `grep -c "AI raw response" src/services/ai.service.ts` returns `0`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit atomic change</name>
  <files>src/services/ai.service.ts</files>
  <action>
Stage only `src/services/ai.service.ts` and commit with message:
```
chore(ai): expand LLM debug logs (finish_reason/refusal/reasoning/usage) + max_tokens 4000

quick-260428-n9u: per-branch debug logging in ai.service.ts to diagnose
empty/truncated DeepSeek responses without re-running. Anthropic branch
now logs stop_reason+usage; OpenAI-compat branch logs finish_reason,
refusal, reasoning_content (truncated 400), tool_calls count, usage and
serialized choice (truncated 1500). Removes legacy 'AI raw response'
log from n29 (superseded). Raises max_tokens 2000 → 4000 in both
branches to rule out truncation.
```

Do NOT use `git add -A` or `git add .` — explicitly: `git add src/services/ai.service.ts`.
Do NOT commit `config/feeds.json` or `prompts/curator.md` (pre-existing modified state from earlier work, unrelated).
Use the gsd-tools commit helper if available, otherwise plain `git commit -m`.
  </action>
  <verify>
    <automated>cd /Users/vladilen/Documents/тнз/club-bot && git log -1 --name-only --pretty=format:"%s" | head -20</automated>
  </verify>
  <done>
- Single new commit on `main` whose subject starts with `chore(ai): expand LLM debug logs`.
- `git show --stat HEAD` lists exactly one file: `src/services/ai.service.ts`.
- `git status` still shows the pre-existing modifications to `config/feeds.json` and `prompts/curator.md` as unstaged (unchanged by this task).
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- `grep -n "Anthropic response (debug)" src/services/ai.service.ts` returns 1 match.
- `grep -n "OpenAI response (debug)" src/services/ai.service.ts` returns 1 match.
- `grep -n "AI raw response" src/services/ai.service.ts` returns 0 matches.
- `grep -nE "max_tokens: 4000" src/services/ai.service.ts` returns 2 matches; `max_tokens: 2000` returns 0.
- `git log -1 --pretty=%s` starts with `chore(ai): expand LLM debug logs`.
</verification>

<success_criteria>
1. Both LLM branches emit a provider-specific debug log line on every `filterArticles` call, capturing finish/stop reasons, usage, refusal/reasoning where applicable.
2. `max_tokens` raised to 4000 in both branches.
3. Legacy n29 `'AI raw response (debug)'` log removed (no duplication).
4. Strict TypeScript preserved — no `any`, type assertions use explicit `{ ... }` shapes.
5. Single atomic commit touches only `src/services/ai.service.ts`.
6. Context budget ≤ 25% (single-file edit, no exploration needed beyond what's embedded in `<interfaces>`).
</success_criteria>

<output>
After completion, no SUMMARY.md required for quick tasks; STATE.md «Quick Tasks Completed» row will be appended by the post-quick wrap-up step (commit hash + date + this directory path).
</output>
